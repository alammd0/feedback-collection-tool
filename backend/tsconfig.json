{
  "compilerOptions": {
    "target": "es2020", // Specifies the ECMAScript target version for the output JavaScript.
    "module": "commonjs", // Specifies the module system for the output JavaScript (e.g., CommonJS for Node.js).
    "outDir": "./dist", // Specifies the output directory for the compiled JavaScript files.
    "rootDir": "./src", // Specifies the root directory of the TypeScript source files.
    "strict": true, // Enables a set of strict type-checking options for better code quality.
    "esModuleInterop": true, // Enables interoperability between CommonJS and ES Modules.
    "skipLibCheck": true, // Skips type checking of declaration files (e.g., node_modules/@types).
    "forceConsistentCasingInFileNames": true // Ensures consistent casing in file names across different operating systems.
  },
  "include": [
    "src/**/*.ts" // Specifies which files to include in the compilation.
  ],
  "exclude": [
    "node_modules" // Specifies which files to exclude from the compilation.
  ]
}